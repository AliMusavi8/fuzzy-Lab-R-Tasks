x <- c(1, 2, 3, 4, 5)
y <- c(2, 4, 6, 8, 10  # Missing closing parenthesis
       
       sum_x <- sum(x)
       sum_y <- sum(y)
       total <- sum_x + sum_y
       print(total)
       

x <- c(1, 2, 3, 4, 5)
y <- c(2, 4, 6, 8, 10)

sum_x <- sum(x)
sum_y <- sum(y)  # Fixed variable name

total <- sum_x + sum_y  # Now references the correct variable

print(total)
